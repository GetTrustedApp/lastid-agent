node_modules/
dist/
.env
.env.local

# Auto-generated by `npm test` on the inner workspace; not source.
plugins/lastid-agent/package-lock.json

# Native libs are committed per-platform in native/, but build outputs
# from a local cross-compile shouldn't pollute the tree.
build/
*.dylib.dSYM
target/
